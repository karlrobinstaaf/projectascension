"""Validator-owned scenario plan factories for Aurora validation.

Modules in this package compile already verified CLI context into governed
harness run plans. The package intentionally performs no eager scenario imports,
so controlled inputs are verified before the CLI resolves a selected factory.
"""

from __future__ import annotations

__all__: tuple[str, ...] = ()
